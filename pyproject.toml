[project]
name = "modal-fish-s2-pro"
version = "0.1.0"
description = "Serverless Fish Audio S2 Pro TTS on Modal"
requires-python = ">=3.10"
dependencies = [
    "modal>=0.73",
    "huggingface-hub>=0.25",
]

[project.scripts]
deploy = "modal deploy modal_app.py"
serve = "modal serve modal_app.py"
